spring:
  application:
    name:student-portal
  datasource:
    url: jdbc:mysql://localhost:3306/students?createDatabaseIfNotExist=true
    username: root
    password: hpone@MySQL12

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port:8080
